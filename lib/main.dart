import 'package:flutter/material.dart';
import 'package:witchle/app/app.dart';

// Executable method
void main() => 
  // Runs app by calling the runApp function with
  // an instance of App class
  runApp(const App()); 